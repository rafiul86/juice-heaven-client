import React from 'react';
import { useParams } from 'react-router';

const Orders = () => {
    // const {id}= useParams()
    return (
        <div>
            <h1>orders</h1>
        </div>
    );
};

export default Orders;